# Run clamd with TCP only
TCPSocket 3310
TCPAddr 0.0.0.0

# Don’t use Unix socket
# LocalSocket /var/run/clamav/clamd.sock

# Database directory
DatabaseDirectory /var/lib/clamav

# Logging (optional, adjust as needed)
LogTime yes
Foreground yes
