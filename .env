# ------------------------------------------------------------------
# DITMail Environment Configuration
#
# IMPORTANT:
# 1. Replace all placeholder values with your own strong, unique secrets after copying this whole into your original .env file.
# 2. NEVER commit your actual .env file to a public Git repository. Add .env to your .gitignore file.
# ------------------------------------------------------------------

# --- MongoDB Credentials ---
# These are used by the MongoDB container on its very first run to create the initial admin user.
# They are also used to build the connection URI for your other services.
MONGO_INITDB_ROOT_USERNAME=ditmail_admin
MONGO_INITDB_ROOT_PASSWORD=ChangeMe_To_A_Very_Strong_Mongo_Password!123

# --- Redis Credentials ---
# This password is used to protect your public-facing Redis instance.
# All services must provide this password to connect.
REDIS_PASSWORD=ChangeMe_To_A_Very_Strong_Redis_Password!456

# --- Application Secrets ---
# This secret key is used by your 'ws' and 'worker' services for signing and verifying JSON Web Tokens (JWTs).
# Make this a long, random, unpredictable string, and has to be same as of frontend's JWT Secret .
JWT_SECRET=ChangeMe_To_A_Long_Random_Unpredictable_JWT_Secret_789
CERTBOT_ENV=--staging
CLOUDFLARE_API_TOKEN=your_token_here
APP_ENCRYPTION_KEY=enc-key-same-as-frontend